@import "tailwindcss";
@import "tw-animate-css";

@theme {
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
  --radius: 0.625rem;
  
  --color-background: hsl(var(--background));
  --color-foreground: hsl(var(--foreground));
  --color-card: hsl(var(--card));
  --color-card-foreground: hsl(var(--card-foreground));
  --color-popover: hsl(var(--popover));
  --color-popover-foreground: hsl(var(--popover-foreground));
  --color-primary: hsl(var(--primary));
  --color-primary-foreground: hsl(var(--primary-foreground));
  --color-secondary: hsl(var(--secondary));
  --color-secondary-foreground: hsl(var(--secondary-foreground));
  --color-muted: hsl(var(--muted));
  --color-muted-foreground: hsl(var(--muted-foreground));
  --color-accent: hsl(var(--accent));
  --color-accent-foreground: hsl(var(--accent-foreground));
  --color-destructive: hsl(var(--destructive));
  --color-destructive-foreground: hsl(var(--destructive-foreground));
  --color-border: hsl(var(--border));
  --color-input: hsl(var(--input));
  --color-ring: hsl(var(--ring));
  --color-sidebar: hsl(var(--sidebar));
  --color-sidebar-foreground: hsl(var(--sidebar-foreground));
  --color-sidebar-primary: hsl(var(--sidebar-primary));
  --color-sidebar-primary-foreground: hsl(var(--sidebar-primary-foreground));
  --color-sidebar-accent: hsl(var(--sidebar-accent));
  --color-sidebar-accent-foreground: hsl(var(--sidebar-accent-foreground));
  --color-sidebar-border: hsl(var(--sidebar-border));
  --color-sidebar-ring: hsl(var(--sidebar-ring));
}

:root {
  /* Light theme: off-white tinted background with deeper neutrals */
  --background: 225 38% 95%;
  --foreground: 225 30% 35%;
  --card: 225 33% 96%;
  --card-foreground: 225 28% 38%;
  --popover: 225 33% 96%;
  --popover-foreground: 225 28% 38%;
  /* Blue primary */
  --primary: 215 85% 55%;
  --primary-foreground: 0 0% 100%;
  /* Neutral secondary */
  --secondary: 225 24% 94%;
  --secondary-foreground: 225 30% 35%;
  /* Muted neutrals */
  --muted: 225 20% 92%;
  --muted-foreground: 225 20% 50%;
  /* Blue accent (subtle for surfaces/hover) */
  --accent: 215 60% 94%;
  --accent-foreground: 225 25% 32%;
  --destructive: 0 72% 50%;
  --destructive-foreground: 0 0% 98%;
  --border: 230 16% 84%;
  --input: 230 16% 84%;
  --ring: 215 85% 55%;
  /* Sidebar tokens */
  --sidebar: 225 24% 93%;
  --sidebar-foreground: 225 30% 35%;
  --sidebar-primary: 215 85% 55%;
  --sidebar-primary-foreground: 0 0% 100%;
  /* Sidebar hover/active background - tuned lighter for consistent look across gradient */
  --sidebar-accent: 225 32% 88%;
  --sidebar-accent-foreground: 225 30% 35%;
  --sidebar-border: 230 16% 84%;
  --sidebar-ring: 215 85% 55%;

  /* Subtle gradients (light mode) */
  /* Sidebar slightly darker than chat with smooth flow */
  --sidebar-gradient-start: 225 36% 95%;
  --sidebar-gradient-end: 225 28% 80%;
  --chat-gradient-start: 225 36% 90%;
  --chat-gradient-end: 225 28% 80%;
}

.dark {
  /* Dark theme: off-gray tinted background and deeper neutrals */
  --background: 220 28% 11%;
  --foreground: 0 0% 100%;
  --card: 220 28% 12%;
  --card-foreground: 0 0% 100%;
  --popover: 220 28% 12%;
  --popover-foreground: 0 0% 100%;
  /* Blue primary */
  --primary: 215 80% 60%;
  --primary-foreground: 0 0% 100%;
  --secondary: 220 16% 16%;
  --secondary-foreground: 0 0% 98%;
  --muted: 220 16% 16%;
  --muted-foreground: 220 12% 72%;
  /* Blue accent (deeper for dark surfaces) */
  --accent: 215 35% 24%;
  --accent-foreground: 0 0% 100%;
  --destructive: 0 84% 60%;
  --destructive-foreground: 0 0% 98%;
  --border: 220 12% 18%;
  --input: 220 12% 18%;
  --ring: 215 80% 60%;
  --sidebar: 220 22% 12%;
  --sidebar-foreground: 240 5% 94%;
  --sidebar-primary: 215 80% 60%;
  --sidebar-primary-foreground: 0 0% 100%;
  --sidebar-accent: 215 28% 20%;
  --sidebar-accent-foreground: 0 0% 100%;
  --sidebar-border: 220 12% 18%;
  --sidebar-ring: 215 80% 60%;

  /* Subtle gradients (dark mode) */
  /* Sidebar darker than chat and more apparent */
  --sidebar-gradient-start: 220 20% 14%;
  --sidebar-gradient-end: 220 20% 9%;
  --chat-gradient-start: 220 30% 15%;
  --chat-gradient-end: 220 30% 8%;
}



@layer base {
  * {
    border-color: hsl(var(--border));
    outline-color: hsl(var(--ring) / 0.5);
  }
  body {
    background-color: hsl(var(--background));
    color: hsl(var(--foreground));
    font-feature-settings: "rlig" 1, "calt" 1;
  }
  /* Prevent iOS Safari zoom on focus by ensuring minimum 16px text size on mobile */
  @media (max-width: 820px) {
    input, textarea, select, button {
      font-size: 16px;
    }
  }
  
  /* Focus styles for better accessibility */
  *:focus-visible {
    outline: 2px solid hsl(var(--ring));
    outline-offset: 2px;
  }
  
  /* Smooth transitions for interactive elements */
  button, input, textarea, select {
    transition: all 0.2s ease-in-out;
  }
  
  /* Improve text contrast */
  h1, h2, h3, h4, h5, h6 {
    color: hsl(var(--foreground));
    font-weight: 600;
  }
}

@layer utilities {
  /* Page headers */
  .page-header {
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .page-title {
    font-weight: 600;
    font-size: 1.25rem;
    letter-spacing: 0.01em;
    color: hsl(var(--foreground));
  }

  /* Account layout gradients */
  .account-gradient {
    background-image:
      radial-gradient(900px 300px at 0% 0%, hsl(var(--accent) / 0.22), transparent 60%),
      radial-gradient(700px 280px at 100% 10%, hsl(var(--primary) / 0.18), transparent 62%),
      linear-gradient(180deg, hsl(var(--chat-gradient-start)), hsl(var(--chat-gradient-end)));
    background-attachment: fixed;
  }

  .dark .account-gradient {
    background-image:
      radial-gradient(900px 320px at 0% 0%, hsl(var(--accent) / 0.18), transparent 60%),
      radial-gradient(700px 300px at 100% 10%, hsl(var(--primary) / 0.22), transparent 62%),
      linear-gradient(180deg, hsl(var(--chat-gradient-start)), hsl(var(--chat-gradient-end)));
  }

  .account-header {
    border-bottom: none;
    background: transparent;
    padding-top: 0.75rem;
  }

  /* Floating header container */
  .floating-header {
    position: relative;
    display: inline-flex;
    align-items: center;
    border-radius: 0.8rem;
    padding: 0.4rem;
    border: 1px solid hsl(var(--primary) / 0.25);
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.14) 0%, hsl(var(--primary) / 0.08) 100%),
      hsl(var(--card));
    backdrop-filter: blur(8px) saturate(1.05);
    -webkit-backdrop-filter: blur(8px) saturate(1.05);
    box-shadow:
      0 14px 36px -18px hsl(var(--foreground) / 0.22),
      inset 0 1px 0 hsl(var(--foreground) / 0.06);
  }
  .dark .floating-header {
    border-color: hsl(var(--primary) / 0.30);
    box-shadow:
      0 14px 36px -18px hsl(var(--foreground) / 0.18),
      inset 0 1px 0 hsl(var(--foreground) / 0.08);
  }

  /* Route tabs for account sections */
  .route-tabs {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.35rem;
    border-radius: 0.75rem;
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.12), hsl(var(--primary) / 0.06)),
      hsl(var(--secondary));
    border: 1px solid hsl(var(--primary) / 0.22);
    box-shadow:
      0 10px 24px -14px hsl(var(--primary) / 0.22),
      inset 0 1px 0 hsl(var(--primary) / 0.14);
  }
  .dark .route-tabs {
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.14), hsl(var(--primary) / 0.08)),
      hsl(var(--sidebar));
    border-color: hsl(var(--primary) / 0.28);
  }
  .route-tab {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 0.9rem;
    border-radius: 0.6rem;
    color: hsl(var(--muted-foreground));
    border: 1px solid transparent;
    transition: all 0.18s ease;
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.06), hsl(var(--primary) / 0.02)),
      hsl(var(--card));
  }
  .route-tab:hover {
    color: hsl(var(--foreground));
    border-color: hsl(var(--primary) / 0.28);
    filter: saturate(1.02);
  }
  .route-tab--active {
    color: hsl(var(--foreground));
    border-color: hsl(var(--primary) / 0.40);
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.16), hsl(var(--primary) / 0.10)),
      hsl(var(--card));
    box-shadow:
      0 12px 28px -16px hsl(var(--primary) / 0.35),
      inset 0 1px 0 hsl(var(--primary) / 0.22);
  }

  /* Mobile floating back action */
  .floating-back {
    position: fixed;
    bottom: 0.75rem;
    right: 0.75rem;
    z-index: 20;
    border: 1px solid hsl(var(--border));
    background: hsl(var(--card));
    border-radius: 0.75rem;
    box-shadow:
      0 14px 36px -18px hsl(var(--foreground) / 0.22),
      inset 0 1px 0 hsl(var(--foreground) / 0.06);
  }
  @media (min-width: 640px) {
    .floating-back { position: static; bottom: auto; right: auto; box-shadow: none; border: none; background: transparent; }
  }

  /* Centered modal primitives */
  .modal-overlay {
    position: fixed;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1rem;
    background: hsl(var(--foreground) / 0.35);
    backdrop-filter: blur(8px) saturate(1.05);
    -webkit-backdrop-filter: blur(8px) saturate(1.05);
    z-index: 50;
    animation: modal-fade-in 160ms ease-out;
  }

  .modal-card {
    width: 100%;
    max-width: 44rem;
    border-radius: 0.9rem;
    border: 1px solid hsl(var(--border));
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.10) 0%,
        hsl(var(--primary) / 0.06) 100%
      ),
      hsl(var(--card));
    box-shadow:
      0 24px 60px -22px hsl(var(--foreground) / 0.25),
      0 8px 26px -10px hsl(var(--foreground) / 0.18),
      inset 0 1px 0 hsl(var(--foreground) / 0.06);
    padding: 1rem;
    max-height: min(80vh, 760px);
    overflow: hidden;
    animation: modal-pop 180ms cubic-bezier(0.22, 0.61, 0.36, 1);
  }

  .modal-close {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    height: 1.75rem;
    width: 1.75rem;
    border-radius: 0.5rem;
    border: 1px solid hsl(var(--border));
    background: hsl(var(--card));
    color: hsl(var(--muted-foreground));
    transition: all 0.18s ease-in-out;
  }
  .modal-close:hover {
    background:
      linear-gradient(
        180deg,
        hsl(var(--accent) / 0.16) 0%,
        hsl(var(--accent) / 0.10) 100%
      ),
      hsl(var(--card));
    border-color: hsl(var(--border));
    color: hsl(var(--foreground));
  }

  @keyframes modal-fade-in {
    from { opacity: 0; }
    to { opacity: 1; }
  }
  @keyframes modal-pop {
    from { transform: translateY(6px) scale(0.98); opacity: 0; }
    to { transform: translateY(0) scale(1); opacity: 1; }
  }
  /* Light-mode surface utilities for inputs, triggers, and menus */
  .surface-input {
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.12) 0%,
        hsl(var(--primary) / 0.05) 100%
      ),
      hsl(var(--card));
    box-shadow:
      0 8px 20px -12px hsl(var(--primary) / 0.28),
      0 2px 6px -2px hsl(var(--foreground) / 0.10),
      inset 0 1px 0 hsl(var(--primary) / 0.12);
  }

  .surface-trigger {
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.14) 0%,
        hsl(var(--primary) / 0.08) 100%
      ),
      hsl(var(--card));
    border-color: hsl(var(--primary) / 0.30) !important;
    box-shadow:
      inset 0 1px 0 hsl(var(--primary) / 0.20),
      0 10px 24px -14px hsl(var(--primary) / 0.22),
      0 4px 10px -6px hsl(var(--foreground) / 0.10);
  }
  .surface-trigger:hover {
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.18) 0%,
        hsl(var(--primary) / 0.12) 100%
      ),
      hsl(var(--card));
    border-color: hsl(var(--primary) / 0.38) !important;
    cursor: pointer;
  }

  .surface-menu {
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.10) 0%,
        hsl(var(--primary) / 0.05) 100%
      ),
      hsl(var(--popover));
    box-shadow:
      0 16px 42px -20px hsl(var(--primary) / 0.32),
      0 8px 22px -10px hsl(var(--foreground) / 0.14),
      inset 0 1px 0 hsl(var(--primary) / 0.10);
    border-color: hsl(var(--primary) / 0.25) !important;
  }

  /* General purpose section card */
  .section-card {
    border-radius: 0.75rem;
    border: 1px solid hsl(var(--primary) / 0.22);
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.10) 0%, hsl(var(--primary) / 0.06) 100%),
      hsl(var(--card));
    box-shadow:
      0 12px 28px -16px hsl(var(--primary) / 0.25),
      0 4px 10px -6px hsl(var(--foreground) / 0.10),
      inset 0 1px 0 hsl(var(--primary) / 0.12);
  }
  .section-card:hover {
    border-color: hsl(var(--primary) / 0.32);
    filter: saturate(1.02);
  }
  .section-card-title {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 600;
    font-size: 1.05rem;
    color: hsl(var(--foreground));
  }

  /* Stat pills (compact, good for usage summaries) */
  .stat-pill {
    border-radius: 0.65rem;
    border: 1px solid hsl(var(--primary) / 0.22);
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.12) 0%, hsl(var(--primary) / 0.06) 100%),
      hsl(var(--card));
    box-shadow:
      0 8px 20px -14px hsl(var(--primary) / 0.28),
      inset 0 1px 0 hsl(var(--primary) / 0.12);
    padding: 0.65rem 0.85rem;
    text-align: center;
    min-width: 5.5rem;
  }
  .stat-pill-label {
    font-size: 0.75rem;
    color: hsl(var(--muted-foreground));
    margin-bottom: 0.2rem;
  }
  .stat-pill-value {
    font-size: 1.25rem;
    font-weight: 700;
    color: hsl(var(--foreground));
  }

  /* Responsive stats row: horizontal on mobile, grid on md+ */
  .stats-row {
    display: flex;
    gap: 0.75rem;
    overflow-x: auto;
    padding: 0 0.25rem 0.25rem 0.25rem;
    -webkit-overflow-scrolling: touch;
    scroll-snap-type: x mandatory;
    justify-content: center;
  }
  .stats-row > * {
    scroll-snap-align: start;
    flex: 0 0 auto;
  }
  @media (min-width: 768px) {
    .stats-row {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 1rem;
      overflow: visible;
      padding: 0;
    }
    .stats-row > * { flex: initial; min-width: 0; }
  }

  /* Info banner for inline notes */
  .info-banner {
    border-radius: 0.65rem;
    border: 1px solid hsl(var(--primary) / 0.25);
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.10) 0%, hsl(var(--primary) / 0.06) 100%),
      hsl(var(--secondary));
    color: hsl(var(--foreground));
    padding: 0.75rem;
  }

  /* Progress variants */
  .danger-progress {
    height: 100%;
    border-radius: 999px;
    background:
      linear-gradient(90deg, hsl(var(--destructive) / 0.9) 0%, hsl(var(--destructive) / 0.7) 100%);
    box-shadow:
      0 6px 14px -6px hsl(var(--destructive) / 0.55),
      inset 0 1px 0 hsl(var(--destructive) / 0.30);
  }

  /* Upgrade card styles for Model Picker */
  .upgrade-card {
    position: relative;
    border-radius: 0.9rem;
    border: 1px solid hsl(var(--primary) / 0.28);
    background:
      radial-gradient(1200px 400px at 0% 0%, hsl(var(--accent) / 0.25), transparent 55%),
      radial-gradient(900px 400px at 100% 10%, hsl(var(--primary) / 0.28), transparent 60%),
      linear-gradient(180deg, hsl(var(--primary) / 0.16) 0%, hsl(var(--primary) / 0.08) 100%),
      hsl(var(--card));
    box-shadow:
      0 18px 42px -20px hsl(var(--primary) / 0.35),
      0 8px 22px -12px hsl(var(--foreground) / 0.18),
      inset 0 1px 0 hsl(var(--primary) / 0.18);
    transition: transform 0.18s ease, box-shadow 0.18s ease, border-color 0.18s ease, filter 0.18s ease;
  }
  @media (max-width: 640px) {
    .upgrade-card {
      padding: 0.6rem 0.75rem;
      border-radius: 0.8rem;
    }
  }
  .upgrade-card:hover {
    border-color: hsl(var(--primary) / 0.40);
    transform: translateY(-1px);
    filter: saturate(1.02);
    box-shadow:
      0 24px 56px -22px hsl(var(--primary) / 0.40),
      0 10px 26px -12px hsl(var(--foreground) / 0.20),
      inset 0 1px 0 hsl(var(--primary) / 0.22);
  }
  .upgrade-card:focus-visible {
    outline: none;
    box-shadow:
      0 0 0 2px hsl(var(--ring) / 0.85),
      0 18px 42px -20px hsl(var(--primary) / 0.35),
      inset 0 1px 0 hsl(var(--primary) / 0.18);
  }
  .dark .upgrade-card {
    background:
      radial-gradient(1200px 420px at 0% 0%, hsl(var(--accent) / 0.20), transparent 55%),
      radial-gradient(900px 420px at 100% 10%, hsl(var(--primary) / 0.35), transparent 60%),
      linear-gradient(180deg, hsl(var(--primary) / 0.18) 0%, hsl(var(--primary) / 0.10) 100%),
      hsl(var(--card));
    border-color: hsl(var(--primary) / 0.34);
    box-shadow:
      0 18px 42px -20px hsl(var(--primary) / 0.28),
      inset 0 1px 0 hsl(var(--primary) / 0.18);
  }
  .upgrade-pill {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    padding: 0.15rem 0.5rem;
    border-radius: 999px;
    font-size: 0.70rem;
    line-height: 1;
    color: hsl(var(--primary-foreground));
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.85) 0%, hsl(var(--primary) / 0.65) 100%);
    box-shadow:
      0 6px 14px -6px hsl(var(--primary) / 0.55),
      inset 0 1px 0 hsl(var(--primary) / 0.30);
  }
  .dark .upgrade-pill {
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.90) 0%, hsl(var(--primary) / 0.70) 100%);
  }
  .upgrade-progress-track {
    height: 0.5rem;
    width: 100%;
    border-radius: 999px;
    border: 1px solid hsl(var(--primary) / 0.35);
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.10) 0%, hsl(var(--primary) / 0.06) 100%),
      hsl(var(--secondary) / 0.9);
    overflow: hidden;
  }
  @media (max-width: 640px) {
    .upgrade-progress-track { height: 0.45rem; }
  }
  .upgrade-progress-fill {
    height: 100%;
    border-radius: 999px;
    background:
      linear-gradient(90deg, hsl(var(--primary) / 0.8) 0%, hsl(var(--primary) / 0.6) 100%);
    box-shadow:
      0 6px 14px -6px hsl(var(--primary) / 0.55),
      inset 0 1px 0 hsl(var(--primary) / 0.30);
  }

  /* Model picker card styles */
  .model-card {
    @apply relative rounded-lg border border-border bg-card p-3 transition-all;
    box-shadow:
      0 6px 16px hsl(var(--foreground) / 0.06),
      inset 0 1px 0 hsl(var(--foreground) / 0.04);
  }
  .model-card:hover {
    @apply border-primary/40;
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.10) 0%,
        hsl(var(--primary) / 0.06) 100%
      ),
      hsl(var(--card));
    box-shadow:
      0 10px 24px -10px hsl(var(--primary) / 0.28),
      0 4px 10px -6px hsl(var(--foreground) / 0.12),
      inset 0 1px 0 hsl(var(--primary) / 0.12);
  }
  .model-card-selected {
    /* Match user-bubble / new chat aesthetic */
    @apply border-primary;
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.10) 0%,
        hsl(var(--primary) / 0.06) 100%
      ),
      hsl(var(--card));
    box-shadow:
      0 8px 20px -12px hsl(var(--primary) / 0.28),
      0 2px 6px -2px hsl(var(--foreground) / 0.10),
      inset 0 1px 0 hsl(var(--primary) / 0.12);
  }
  .model-card-selected:hover {
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.12) 0%,
        hsl(var(--primary) / 0.08) 100%
      ),
      hsl(var(--card));
    border-color: hsl(var(--primary) / 0.30);
  }
  .model-card-wide {
    @apply p-4;
  }
  /* File support badge */
  .badge-file {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    padding: 0.25rem 0.5rem;
    border-radius: 0.5rem;
    border: 1px solid hsl(var(--primary) / 0.35);
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.18) 0%,
        hsl(var(--primary) / 0.10) 100%
      ),
      hsl(var(--card));
    color: hsl(var(--foreground));
    font-size: 0.70rem;
    line-height: 1;
    white-space: nowrap;
    box-shadow:
      0 10px 24px -14px hsl(var(--primary) / 0.28),
      0 4px 10px -6px hsl(var(--foreground) / 0.10),
      inset 0 1px 0 hsl(var(--primary) / 0.12);
  }
  .badge-file svg {
    filter: drop-shadow(0 1px 0 hsl(var(--foreground) / 0.15));
  }
  /* Primary badges inline with model name - keep original colors */
  .badge-primary {
    @apply inline-flex items-center gap-1 rounded-md bg-primary text-primary-foreground px-2.5 py-1 text-[11px] shadow;
    white-space: nowrap;
  }

  .badge-set-primary {
    @apply inline-flex items-center gap-1 rounded-md bg-background border border-border px-2.5 py-1 text-[11px] shadow cursor-pointer;
    white-space: nowrap;
  }
  /* Subtle gradients for sidebar and chat surfaces */
  .brand-sidebar [data-slot="sidebar-inner"],
  .pink-sidebar [data-slot="sidebar-inner"] {
    background-color: hsl(var(--sidebar));
    background-image: linear-gradient(
      135deg,
      hsl(var(--sidebar-gradient-start)) 0%,
      hsl(var(--sidebar-gradient-end)) 100%
    );
    border-right: 1px solid hsl(var(--sidebar-border));
    border-radius: 0.75rem; /* 12px rounded corners */
    /* Stronger light-mode shadows to avoid looking flat */
    box-shadow:
      0 10px 25px hsl(var(--foreground) / 0.08),
      0 2px 8px hsl(var(--foreground) / 0.12),
      0 8px 20px hsl(var(--primary) / 0.08),
      inset 0 1px 0 hsl(var(--foreground) / 0.06);
  }

  /* Preserve subtler shadows in dark mode */
  .dark .brand-sidebar [data-slot="sidebar-inner"],
  .dark .pink-sidebar [data-slot="sidebar-inner"] {
    box-shadow:
      0 10px 25px hsl(var(--foreground) / 0.04),
      0 2px 8px hsl(var(--foreground) / 0.06),
      0 8px 20px hsl(var(--primary) / 0.06),
      inset 0 1px 0 hsl(var(--foreground) / 0.05);
  }

  [data-slot="sidebar-inset"].brand-chat,
  [data-slot="sidebar-inset"].pink-chat {
    /* Same background as sidebar */
    background-color: hsl(var(--sidebar));
    background-image: linear-gradient(
      135deg,
      hsl(var(--sidebar-gradient-start)) 0%,
      hsl(var(--sidebar-gradient-end)) 100%
    );
    /* Same styling as sidebar */
    border-right: 1px solid hsl(var(--sidebar-border));
    border-radius: 0.75rem;
    /* Add spacing around like sidebar */
    margin: 0.5rem;
    /* Calculate height accounting for margin */
    height: calc(100vh - 1rem);
    height: calc(100dvh - 1rem);
    /* Same shadows as sidebar - stronger light-mode shadows */
    box-shadow:
      0 10px 25px hsl(var(--foreground) / 0.08),
      0 2px 8px hsl(var(--foreground) / 0.12),
      0 8px 20px hsl(var(--primary) / 0.08),
      inset 0 1px 0 hsl(var(--foreground) / 0.06);
    /* Ensure it's non-scrollable */
    overflow: hidden;
  }

  .dark [data-slot="sidebar-inset"].brand-chat,
  .dark [data-slot="sidebar-inset"].pink-chat {
    /* Same background as sidebar in dark mode */
    background-color: hsl(var(--sidebar));
    background-image: linear-gradient(
      135deg,
      hsl(var(--sidebar-gradient-start)) 0%,
      hsl(var(--sidebar-gradient-end)) 100%
    );
    /* Same styling as sidebar */
    border-right: 1px solid hsl(var(--sidebar-border));
    border-radius: 0.75rem;
    /* Add spacing around like sidebar */
    margin: 0.5rem;
    /* Calculate height accounting for margin */
    height: calc(100vh - 1rem);
    height: calc(100dvh - 1rem);
    /* Same shadows as sidebar - subtler dark mode shadows */
    box-shadow:
      0 10px 25px hsl(var(--foreground) / 0.04),
      0 2px 8px hsl(var(--foreground) / 0.06),
      0 8px 20px hsl(var(--primary) / 0.06),
      inset 0 1px 0 hsl(var(--foreground) / 0.05);
    /* Ensure it's non-scrollable */
    overflow: hidden;
  }

  /* Mobile sidebar sheet */
  .brand-sidebar [data-mobile="true"] {
    background-color: hsl(var(--sidebar));
    background-image: linear-gradient(
      180deg,
      hsl(var(--sidebar-gradient-start)) 0%,
      hsl(var(--sidebar-gradient-end)) 100%
    );
  }

  /* Ensure html and body take full height; prefer dynamic viewport units on mobile */
  html, body {
    height: 100%;
    height: 100dvh;
    overflow: hidden;
  }

  /* Lock the main layout container */
  [data-sidebar-provider] {
    height: 100vh;
    height: 100dvh; /* iOS Safari dynamic viewport */
    overflow: hidden;
  }

  /* Lock the root app container */
  #__next, [data-nextjs-scroll-focus-boundary] {
    height: 100%;
    height: 100dvh;
    overflow: hidden;
  }

  /* Custom scrollbar for webkit browsers */
  .custom-scrollbar::-webkit-scrollbar {
    width: 6px;
  }
  
  .custom-scrollbar::-webkit-scrollbar-track {
    background: transparent;
  }
  
  .custom-scrollbar::-webkit-scrollbar-thumb {
    background: hsl(var(--border));
    border-radius: 3px;
  }
  
  .custom-scrollbar::-webkit-scrollbar-thumb:hover {
    background: hsl(var(--border) / 0.8);
  }

  /* Hidden scrollbar for chat section */
  .chats-scroll {
    overflow-y: auto;
    scrollbar-width: none; /* Firefox */
    -ms-overflow-style: none; /* IE/Edge */
  }
  
  .chats-scroll::-webkit-scrollbar {
    display: none; /* Chrome/Safari/Webkit */
  }

  /* Icon button utility class */
  .icon-button {
    @apply flex h-8 w-full items-center justify-start gap-2 rounded-md p-2 text-sm outline-hidden transition-colors;
    @apply hover:bg-sidebar-accent hover:text-sidebar-accent-foreground;
    @apply focus-visible:ring-2 focus-visible:ring-sidebar-ring;
    @apply group-data-[collapsible=icon]:justify-center group-data-[collapsible=icon]:size-8;
  }
  
  /* Improved button hover states */
  .btn-primary {
    @apply bg-primary text-primary-foreground hover:bg-primary/90 focus-visible:ring-2 focus-visible:ring-ring transition-colors;
  }
  
  .btn-secondary {
    @apply bg-secondary text-secondary-foreground hover:bg-secondary/80 focus-visible:ring-2 focus-visible:ring-ring transition-colors;
  }
  
  .btn-destructive {
    @apply bg-destructive text-destructive-foreground hover:bg-destructive/85 focus-visible:ring-2 focus-visible:ring-ring transition-colors;
    box-shadow:
      0 12px 28px -14px hsl(var(--destructive) / 0.35),
      0 4px 10px -2px hsl(var(--destructive) / 0.25);
    border: 1px solid hsl(var(--destructive) / 0.45);
  }
  
  /* Card hover effects */
  .card-hover {
    @apply transition-colors hover:bg-card/80;
  }
  
  /* Input focus improvements */
  .input-focus {
    @apply focus:outline-none focus:ring-2 focus:ring-ring focus:border-ring transition-colors;
  }

  /* Beautiful New Chat Button */
  .btn-new-chat {
    position: relative;
    width: 100%;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.75rem 1rem;
    border-radius: 0.75rem;
    font-weight: 600;
    font-size: 0.875rem;
    transition: all 0.2s ease;
    cursor: pointer;
    overflow: hidden;
    /* More prominent border for better visibility */
    border: 1px solid hsl(var(--primary) / 0.25);
    /* Enhanced gradient with higher opacity for more prominence */
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.15) 0%,
        hsl(var(--primary) / 0.10) 55%,
        hsl(var(--primary) / 0.08) 100%
      ),
      linear-gradient(135deg, hsl(var(--primary) / 0.18), hsl(var(--accent) / 0.15));
    color: hsl(var(--primary));
    /* Enhanced shadows for more depth and prominence */
    box-shadow:
      inset 0 1px 0 0 hsl(var(--primary) / 0.25),
      inset 0 -1px 0 0 hsl(var(--primary) / 0.12),
      0 12px 28px -14px hsl(var(--primary) / 0.25),
      0 4px 10px -2px hsl(var(--primary) / 0.15);
  }

  .btn-new-chat:hover {
    border-color: hsl(var(--primary) / 0.35);
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.20) 0%,
        hsl(var(--primary) / 0.15) 52%,
        hsl(var(--primary) / 0.12) 100%
      ),
      linear-gradient(135deg, hsl(var(--primary) / 0.22), hsl(var(--accent) / 0.18));
    box-shadow:
      inset 0 1px 0 0 hsl(var(--primary) / 0.30),
      inset 0 -1px 0 0 hsl(var(--primary) / 0.15),
      0 16px 32px -14px hsl(var(--primary) / 0.30),
      0 6px 12px -3px hsl(var(--primary) / 0.18);
    /* Removed transform: translateY(-1px) to eliminate the raising effect */
  }

  .btn-new-chat:focus-visible {
    outline: none;
    box-shadow: 0 0 0 2px hsl(var(--primary) / 0.45);
  }

  /* Dark mode variant */
  .dark .btn-new-chat {
    background: 
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.15) 0%,
        hsl(var(--primary) / 0.08) 55%,
        hsl(var(--primary) / 0.05) 100%
      ),
      hsl(var(--sidebar));
    color: hsl(var(--primary));
    border: 1px solid hsl(var(--primary) / 0.2);
    box-shadow: 
      0 20px 50px -20px hsl(var(--primary) / 0.3),
      inset 0 1px 0 0 hsl(var(--primary) / 0.1);
  }

  .dark .btn-new-chat:hover {
    border-color: hsl(var(--primary) / 0.3);
    background: 
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.20) 0%,
        hsl(var(--primary) / 0.12) 55%,
        hsl(var(--primary) / 0.08) 100%
      ),
      hsl(var(--sidebar));
  }

  /* Compact variant of new chat button for send action */
  .btn-new-chat-compact {
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 0.6rem;
    border: 1px solid hsl(var(--primary) / 0.4);
    cursor: pointer;
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.24) 0%,
        hsl(var(--primary) / 0.16) 58%,
        hsl(var(--primary) / 0.14) 100%
      ),
      linear-gradient(135deg, hsl(var(--primary) / 0.28), hsl(var(--accent) / 0.22));
    color: hsl(var(--primary-foreground));
    box-shadow:
      inset 0 1px 0 0 hsl(var(--primary) / 0.35),
      inset 0 -1px 0 0 hsl(var(--primary) / 0.22),
      0 14px 36px -16px hsl(var(--primary) / 0.45),
      0 6px 14px -4px hsl(var(--primary) / 0.28),
      0 0 0 2px hsl(var(--primary) / 0.2);
    transition: box-shadow 0.18s ease, transform 0.18s ease, border-color 0.18s ease;
  }
  .btn-new-chat-compact:hover {
    border-color: hsl(var(--primary) / 0.5);
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.36) 0%,
        hsl(var(--primary) / 0.28) 58%,
        hsl(var(--primary) / 0.22) 100%
      ),
      linear-gradient(135deg, hsl(var(--primary) / 0.32), hsl(var(--accent) / 0.26));
    box-shadow:
      inset 0 1px 0 0 hsl(var(--primary) / 0.38),
      inset 0 -1px 0 0 hsl(var(--primary) / 0.24),
      0 18px 42px -16px hsl(var(--primary) / 0.55),
      0 8px 18px -6px hsl(var(--primary) / 0.34),
      0 0 0 3px hsl(var(--primary) / 0.28);
    filter: brightness(0.96) saturate(1.03);
  }
  .btn-new-chat-compact:focus-visible {
    outline: none;
    box-shadow:
      inset 0 1px 0 0 hsl(var(--primary) / 0.4),
      inset 0 -1px 0 0 hsl(var(--primary) / 0.26),
      0 18px 42px -16px hsl(var(--primary) / 0.55),
      0 8px 18px -6px hsl(var(--primary) / 0.34),
      0 0 0 3px hsl(var(--primary) / 0.35);
  }
  .btn-new-chat-compact svg {
    filter: drop-shadow(0 1px 0 hsl(var(--foreground) / 0.15));
  }
  .dark .btn-new-chat-compact {
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.22) 0%,
        hsl(var(--primary) / 0.12) 60%,
        hsl(var(--primary) / 0.08) 100%
      ),
      hsl(var(--sidebar));
    border: 1px solid hsl(var(--primary) / 0.3);
    box-shadow:
      0 16px 40px -22px hsl(var(--primary) / 0.32),
      inset 0 1px 0 0 hsl(var(--primary) / 0.18);
  }
  .dark .btn-new-chat-compact:hover {
    border-color: hsl(var(--primary) / 0.4);
    filter: none;
  }
}

@layer utilities {
  /* OAuth button */
  .btn-oauth {
    position: relative;
    width: 100%;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.625rem 0.75rem;
    border-radius: var(--radius);
    font-weight: 600;
    transition: all 0.2s ease;
    cursor: pointer;
    overflow: hidden;
    border: 1px solid rgba(0, 0, 0, 0.12);
    background:
      linear-gradient(
        180deg,
        rgba(255, 255, 255, 0.99) 0%,
        rgba(246, 248, 255, 0.97) 55%,
        rgba(255, 251, 243, 0.95) 100%
      ),
      linear-gradient(135deg, hsl(233 85% 60% / 0.10), hsl(43 74% 52% / 0.08));
    color: hsl(var(--foreground));
    box-shadow:
      inset 0 1px 0 0 rgba(255, 255, 255, 0.85),
      inset 0 -1px 0 0 hsl(233 85% 60% / 0.07),
      0 12px 28px -14px rgba(0, 0, 0, 0.18),
      0 2px 6px -2px rgba(0, 0, 0, 0.08);
  }

  .btn-oauth:hover {
    border-color: hsl(233 85% 60% / 0.22);
    background:
      linear-gradient(
        180deg,
        rgba(255, 255, 255, 1) 0%,
        rgba(244, 246, 255, 0.98) 52%,
        rgba(255, 249, 239, 0.96) 100%
      ),
      linear-gradient(135deg, hsl(233 85% 60% / 0.14), hsl(43 74% 52% / 0.12));
    box-shadow:
      inset 0 1px 0 0 rgba(255, 255, 255, 0.9),
      inset 0 -1px 0 0 hsl(233 85% 60% / 0.10),
      0 18px 40px -14px rgba(0, 0, 0, 0.22),
      0 3px 10px -3px rgba(0, 0, 0, 0.08),
      0 10px 26px -14px hsl(233 85% 60% / 0.28);
    transform: translateY(-1px);
  }

  .btn-oauth:focus-visible {
    outline: none;
    box-shadow: 0 0 0 2px hsla(233, 85%, 60%, 0.45);
  }

  .dark .btn-oauth {
    background: hsl(220 28% 12%);
    color: white;
    border: 1px solid rgba(255, 255, 255, 0.1);
    box-shadow: 0 20px 50px -20px rgba(0, 0, 0, 0.6);
  }

  .dark .btn-oauth:hover {
    border-color: rgba(255, 255, 255, 0.2);
  }

  /* Google variant: subtle brand tint overlay */
  .btn-oauth--google {
    background:
      linear-gradient(
        180deg,
        rgba(255, 255, 255, 0.99) 0%,
        rgba(246, 248, 255, 0.97) 55%,
        rgba(255, 251, 243, 0.95) 100%
      ),
      linear-gradient(135deg, hsl(233 85% 60% / 0.10), hsl(43 74% 52% / 0.08));
  }

  .btn-oauth--google:hover {
    background:
      linear-gradient(
        180deg,
        rgba(255, 255, 255, 1) 0%,
        rgba(244, 246, 255, 0.98) 52%,
        rgba(255, 249, 239, 0.96) 100%
      ),
      linear-gradient(135deg, hsl(233 85% 60% / 0.14), hsl(43 74% 52% / 0.12));
  }

  .dark .btn-oauth--google {
    background:
      linear-gradient(180deg, hsl(220 28% 12%), hsl(220 28% 12%));
  }

  .oauth-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    height: 20px;
    width: 20px;
    border-radius: 999px;
    background: white;
    border: 1px solid rgba(0, 0, 0, 0.08);
    box-shadow: 0 1px 0 rgba(0, 0, 0, 0.06);
  }

  .dark .oauth-icon {
    background: #111318;
    border-color: rgba(255, 255, 255, 0.12);
  }
}

/* Chat message styling and attachments */
@layer utilities {
  .user-bubble {
    color: hsl(var(--foreground));
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.06) 0%,
        hsl(var(--primary) / 0.03) 100%
      ),
      hsl(var(--card));
    border: 1px solid hsl(var(--primary) / 0.20);
    box-shadow:
      0 8px 20px -12px hsl(var(--primary) / 0.28),
      0 2px 6px -2px hsl(var(--foreground) / 0.10),
      inset 0 1px 0 hsl(var(--primary) / 0.12);
  }
  .user-bubble:hover {
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.08) 0%,
        hsl(var(--primary) / 0.04) 100%
      ),
      hsl(var(--card));
    border-color: hsl(var(--primary) / 0.25);
  }

  .assistant-text { font-size: 1rem; line-height: 1.85; color: hsl(var(--foreground)); }

  .attachment-card {
    background:
      linear-gradient(
        180deg,
        hsl(var(--accent) / 0.10) 0%,
        hsl(var(--accent) / 0.06) 100%
      );
    border: 1px solid hsl(var(--border));
    box-shadow:
      0 8px 20px hsl(var(--foreground) / 0.06),
      inset 0 1px 0 hsl(var(--foreground) / 0.05);
  }
  .attachment-card-user {
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.10) 0%,
        hsl(var(--primary) / 0.06) 100%
      );
    border: 1px solid hsl(var(--primary) / 0.25);
  }
  .attachment-image {
    box-shadow:
      0 10px 24px hsl(var(--foreground) / 0.10),
      0 2px 8px hsl(var(--foreground) / 0.12);
    border: 1px solid hsl(var(--border));
  }

  .chat-content {
    padding-top: 1rem;
  }

  /* Summary table UI */
  .summary-table th, .summary-table td {
    padding: 0.75rem 0.75rem;
    border-bottom: 1px solid hsl(var(--border));
    vertical-align: top;
  }
  .summary-table thead th {
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.10) 0%,
        hsl(var(--primary) / 0.06) 100%
      );
    border-bottom: 1px solid hsl(var(--primary) / 0.25);
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }
  .summary-table tbody tr:hover {
    background: hsl(var(--accent) / 0.25);
    transition: background 0.18s ease;
  }
  /* Column sizing to avoid squished model names on larger screens */
  .summary-col-model { min-width: 220px; width: 24%; }
  .summary-col-initial { width: 26%; }
  .summary-col-refined { width: 26%; }
  .summary-col-changed { width: 10%; }
  .summary-col-keypoints { width: 14%; }
  /* Muted body cells for better hierarchy */
  .summary-cell-muted { color: hsl(var(--muted-foreground)); }
  .table-chip-group { display: flex; flex-wrap: wrap; gap: 0.4rem; }
  .table-chip {
    display: inline-flex;
    align-items: center;
    padding: 0.2rem 0.5rem;
    border-radius: 0.5rem;
    border: 1px solid hsl(var(--border));
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.08) 0%,
        hsl(var(--primary) / 0.04) 100%
      ),
      hsl(var(--card));
    font-size: 0.75rem;
  }
  /* Roomier chips for agreements/disagreements */
  .table-chip--roomy { padding: 0.35rem 0.65rem; }
  .summary-chip { color: hsl(var(--muted-foreground)); }
  .chip-destructive {
    border-color: hsl(var(--destructive) / 0.35);
    background:
      linear-gradient(
        180deg,
        hsl(var(--destructive) / 0.12) 0%,
        hsl(var(--destructive) / 0.06) 100%
      ),
      hsl(var(--card));
    color: hsl(var(--destructive));
  }
  .table-tile { @apply rounded-lg border border-border bg-card p-3; }
  .table-tile-title { @apply text-xs font-medium text-muted-foreground; }
  .table-tile-body { @apply mt-1; }

  /* Mobile model summary card layout */
  .model-summary-card {
    border: 1px solid hsl(var(--border));
    border-radius: 0.75rem;
    padding: 0.75rem;
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.08) 0%,
        hsl(var(--primary) / 0.04) 100%
      ),
      hsl(var(--card));
    box-shadow:
      0 6px 16px hsl(var(--foreground) / 0.06),
      inset 0 1px 0 hsl(var(--foreground) / 0.04);
  }
  .msc-header { display: flex; align-items: center; justify-content: space-between; gap: 0.75rem; }
  .msc-model { display: flex; align-items: center; gap: 0.5rem; min-width: 0; }
  .msc-model-name { font-weight: 600; overflow-wrap: anywhere; }
  .msc-section { margin-top: 0.5rem; }
  .summary-label { font-size: 0.7rem; letter-spacing: 0.04em; text-transform: uppercase; color: hsl(var(--muted-foreground)); }
  .summary-value { margin-top: 0.25rem; font-size: 0.9rem; color: hsl(var(--muted-foreground)); white-space: pre-wrap; }

  /* Toggle button */
  .toggle-btn {
    padding: 0.35rem 0.65rem;
    font-size: 0.75rem;
    border: 1px solid hsl(var(--primary) / 0.35);
    border-radius: 0.5rem;
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.14) 0%, hsl(var(--primary) / 0.08) 100%),
      hsl(var(--card));
    color: hsl(var(--foreground));
    transition: all 0.18s ease;
  }
  .toggle-btn:hover { border-color: hsl(var(--primary) / 0.45); filter: brightness(0.98); }

  /* Collapsed whole-run card */
  .collapsed-card {
    border: 1px dashed hsl(var(--primary) / 0.45);
    border-radius: 0.75rem;
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.10) 0%, hsl(var(--primary) / 0.06) 100%),
      hsl(var(--card));
    padding: 0.75rem;
    cursor: pointer;
    box-shadow:
      0 8px 20px -12px hsl(var(--primary) / 0.28),
      0 2px 6px -2px hsl(var(--foreground) / 0.10),
      inset 0 1px 0 hsl(var(--primary) / 0.12);
  }
  .collapsed-card:hover { border-color: hsl(var(--primary)); }
  .collapsed-card-row { display: flex; align-items: center; gap: 0.75rem; }
  .collapsed-phase { display: inline-flex; align-items: center; gap: 0.5rem; }
  .collapsed-phase-icon { display: inline-flex; align-items: center; justify-content: center; height: 1.25rem; width: 1.25rem; }
  .collapsed-phase-text { font-size: 0.85rem; transition: color 120ms ease; }
  .collapsed-sep { width: 1px; height: 1.2rem; background: hsl(var(--border)); }
  .collapsed-hint { margin-top: 0.5rem; font-size: 0.7rem; color: hsl(var(--muted-foreground)); }

  /* Final summary compact card */
  .final-compact-card {
    border: 1px solid hsl(var(--border));
    border-radius: 0.75rem;
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.08) 0%, hsl(var(--primary) / 0.04) 100%),
      hsl(var(--card));
    padding: 0.75rem;
    box-shadow:
      0 8px 20px -12px hsl(var(--primary) / 0.28),
      0 2px 6px -2px hsl(var(--foreground) / 0.10),
      inset 0 1px 0 hsl(var(--primary) / 0.12);
  }
  .final-compact-title { display: flex; align-items: center; gap: 0.4rem; font-weight: 600; font-size: 0.85rem; }
  .final-compact-body { margin-top: 0.25rem; color: hsl(var(--muted-foreground)); font-size: 0.9rem; }
  
  /* Beams overlay helper to blend lines softly above content */
  .beams-overlay {
    mix-blend-mode: normal;
    pointer-events: none;
  }
}

@layer utilities {
  /* Subscription accordion */
  .subscription-accordion {
    border-radius: 0.75rem;
    border: 1px solid hsl(var(--primary) / 0.22);
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.10) 0%, hsl(var(--primary) / 0.06) 100%),
      hsl(var(--card));
    box-shadow:
      0 12px 28px -16px hsl(var(--primary) / 0.25),
      0 4px 10px -6px hsl(var(--foreground) / 0.10),
      inset 0 1px 0 hsl(var(--primary) / 0.12);
    transition: border-color 0.18s ease, filter 0.18s ease, box-shadow 0.18s ease;
  }
  .subscription-accordion:hover { border-color: hsl(var(--primary) / 0.32); filter: saturate(1.02); }
  .subscription-accordion-header {
    display: flex;
    align-items: start;
    justify-content: space-between;
    gap: 0.5rem;
    padding: 0.9rem 1rem;
    border-radius: 0.75rem;
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.12) 0%, hsl(var(--primary) / 0.06) 100%),
      hsl(var(--card));
    border: 1px solid hsl(var(--primary) / 0.18);
    box-shadow: inset 0 1px 0 hsl(var(--primary) / 0.10);
  }
  .subscription-accordion-subtitle {
    margin-top: 0.35rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: hsl(var(--muted-foreground));
  }
  .subscription-accordion-content {
    max-height: 0;
    overflow: hidden;
    opacity: 0;
    transform: translateY(-4px);
    transition: max-height 220ms ease, opacity 180ms ease, transform 200ms ease, padding 200ms ease;
    padding: 0 1rem;
  }
  .subscription-accordion.accordion-open .subscription-accordion-content {
    max-height: 520px;
    opacity: 1;
    transform: translateY(0);
    padding: 0.75rem 1rem 1rem 1rem;
  }
  
  /* Desktop hover enhancement - only applies when not using touch */
  @media (hover: hover) and (pointer: fine) {
    .subscription-accordion:hover .subscription-accordion-content {
      max-height: 520px;
      opacity: 1;
      transform: translateY(0);
      padding: 0.75rem 1rem 1rem 1rem;
    }
  }

  /* Pills */
  .pill {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    padding: 0.15rem 0.5rem;
    border-radius: 999px;
    font-size: 0.7rem;
    line-height: 1;
    border: 1px solid hsl(var(--border));
    box-shadow:
      0 6px 14px -6px hsl(var(--foreground) / 0.18),
      inset 0 1px 0 hsl(var(--foreground) / 0.08);
  }
  .pill-success {
    color: hsl(var(--primary-foreground));
    background:
      linear-gradient(180deg, hsl(150 65% 40% / 0.90) 0%, hsl(150 65% 34% / 0.90) 100%);
    border-color: hsl(150 65% 36% / 0.85);
  }
  .pill-danger {
    color: hsl(var(--destructive-foreground));
    background:
      linear-gradient(180deg, hsl(var(--destructive) / 0.90) 0%, hsl(var(--destructive) / 0.80) 100%);
    border-color: hsl(var(--destructive));
  }

  /* Pricing cards */
  .pricing-card {
    position: relative;
    border-radius: 0.9rem;
    margin-top: .5rem;
    border: 1px solid hsl(var(--primary) / 0.28);
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.12) 0%, hsl(var(--primary) / 0.06) 100%),
      hsl(var(--card));
    padding: 1rem;
    box-shadow:
      0 12px 28px -16px hsl(var(--primary) / 0.28),
      0 4px 12px -8px hsl(var(--foreground) / 0.14),
      inset 0 1px 0 hsl(var(--primary) / 0.12);
    transition: transform 0.18s ease, box-shadow 0.18s ease, border-color 0.18s ease, filter 0.18s ease;
  }
  .pricing-card:hover {
    transform: translateY(-2px);
    border-color: hsl(var(--primary) / 0.40);
    filter: saturate(1.02);
    box-shadow:
      0 20px 50px -24px hsl(var(--primary) / 0.40),
      0 6px 18px -8px hsl(var(--foreground) / 0.16),
      inset 0 1px 0 hsl(var(--primary) / 0.16);
  }
  .pricing-card-current { border-color: hsl(var(--primary)); }
  .pricing-card-featured {
    border-color: hsl(var(--primary) / 0.5);
    background:
      radial-gradient(900px 400px at 100% 0%, hsl(var(--primary) / 0.20), transparent 60%),
      linear-gradient(180deg, hsl(var(--primary) / 0.16) 0%, hsl(var(--primary) / 0.08) 100%),
      hsl(var(--card));
    box-shadow:
      0 24px 56px -26px hsl(var(--primary) / 0.45),
      0 10px 26px -16px hsl(var(--foreground) / 0.18),
      inset 0 1px 0 hsl(var(--primary) / 0.16);
  }
  .dark .pricing-card-featured {
    background:
      radial-gradient(900px 420px at 100% 0%, hsl(var(--primary) / 0.25), transparent 60%),
      linear-gradient(180deg, hsl(var(--primary) / 0.18) 0%, hsl(var(--primary) / 0.10) 100%),
      hsl(var(--card));
  }
  .pricing-card-header { display: grid; gap: 0.25rem; margin-bottom: 0.75rem; }
  .pricing-title { font-weight: 700; font-size: 1.05rem; color: hsl(var(--foreground)); }
  .badge-popular {
    justify-self: start;
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    padding: 0.2rem 0.5rem;
    border-radius: 999px;
    font-size: 0.70rem;
    line-height: 1;
    color: hsl(var(--primary-foreground));
    background:
      linear-gradient(180deg, hsl(var(--primary) / 0.85) 0%, hsl(var(--primary) / 0.65) 100%);
    box-shadow:
      0 6px 14px -6px hsl(var(--primary) / 0.55),
      inset 0 1px 0 hsl(var(--primary) / 0.30);
  }
  .pricing-price { display: flex; align-items: baseline; gap: 0.25rem; margin-top: 0.25rem; }
  .pricing-amount { font-size: 2rem; font-weight: 800; letter-spacing: -0.02em;  }
  .pricing-period { font-size: 0.85rem; color: hsl(var(--muted-foreground)); }
  .pricing-features { margin: 0.5rem 0 0.9rem; display: grid; gap: 0.35rem; color: hsl(var(--muted-foreground)); }
  .pricing-features li { position: relative; padding-left: 1.45rem;   }
  .pricing-features li::before {
    content: "✔";
    position: absolute;
    left: 0;
    top: 0.05rem;
    color: hsl(var(--primary));
    text-shadow: 0 1px 0 hsl(var(--foreground) / 0.15);
  }

  /* Pricing Button Styles */
  .btn-pricing {
    position: relative;
    width: 100%;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    border-radius: 0.7rem;
    font-weight: 600;
    font-size: 0.875rem;
    letter-spacing: 0.01em;
    transition: all 0.18s ease;
    cursor: pointer;
    overflow: hidden;
    border: 1px solid hsl(var(--primary) / 0.35);
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.18) 0%,
        hsl(var(--primary) / 0.12) 52%,
        hsl(var(--primary) / 0.08) 100%
      ),
      linear-gradient(135deg, hsl(var(--primary) / 0.22), hsl(var(--accent) / 0.18));
    color: hsl(var(--primary));
    box-shadow:
      inset 0 1px 0 0 hsl(var(--primary) / 0.30),
      inset 0 -1px 0 0 hsl(var(--primary) / 0.15),
      0 12px 28px -14px hsl(var(--primary) / 0.35),
      0 4px 10px -2px hsl(var(--primary) / 0.20);
  }

  .btn-pricing:hover {
    border-color: hsl(var(--primary) / 0.45);
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.24) 0%,
        hsl(var(--primary) / 0.16) 52%,
        hsl(var(--primary) / 0.12) 100%
      ),
      linear-gradient(135deg, hsl(var(--primary) / 0.28), hsl(var(--accent) / 0.22));
    box-shadow:
      inset 0 1px 0 0 hsl(var(--primary) / 0.35),
      inset 0 -1px 0 0 hsl(var(--primary) / 0.18),
      0 16px 32px -14px hsl(var(--primary) / 0.40),
      0 6px 12px -3px hsl(var(--primary) / 0.25);
    transform: translateY(-1px);
    filter: saturate(1.02);
  }

  .btn-pricing:focus-visible {
    outline: none;
    box-shadow:
      0 0 0 2px hsl(var(--background)),
      0 0 0 4px hsl(var(--primary) / 0.50),
      inset 0 1px 0 0 hsl(var(--primary) / 0.30);
  }

  .btn-pricing:disabled {
    opacity: 0.65;
    cursor: not-allowed;
    transform: none;
    filter: none;
  }

  /* Primary variant - for upgrade buttons */
  .btn-pricing-primary {
    border-color: hsl(var(--primary) / 0.45);
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.90) 0%,
        hsl(var(--primary) / 0.75) 52%,
        hsl(var(--primary) / 0.65) 100%
      );
    color: hsl(var(--primary-foreground));
    box-shadow:
      inset 0 1px 0 0 hsl(var(--primary) / 0.35),
      inset 0 -1px 0 0 hsl(var(--primary) / 0.20),
      0 14px 32px -16px hsl(var(--primary) / 0.45),
      0 6px 14px -4px hsl(var(--primary) / 0.30);
  }

  .btn-pricing-primary:hover {
    border-color: hsl(var(--primary) / 0.55);
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.95) 0%,
        hsl(var(--primary) / 0.85) 52%,
        hsl(var(--primary) / 0.75) 100%
      );
    box-shadow:
      inset 0 1px 0 0 hsl(var(--primary) / 0.40),
      inset 0 -1px 0 0 hsl(var(--primary) / 0.25),
      0 18px 42px -16px hsl(var(--primary) / 0.55),
      0 8px 18px -6px hsl(var(--primary) / 0.35);
  }

  /* Featured variant - for pro plan */
  .btn-pricing-featured {
    border-color: hsl(var(--primary) / 0.50);
    background:
      radial-gradient(800px 300px at 50% 0%, hsl(var(--primary) / 0.25), transparent 70%),
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.22) 0%,
        hsl(var(--primary) / 0.14) 52%,
        hsl(var(--primary) / 0.10) 100%
      ),
      linear-gradient(135deg, hsl(var(--primary) / 0.30), hsl(var(--accent) / 0.25));
    color: hsl(var(--primary));
    box-shadow:
      inset 0 1px 0 0 hsl(var(--primary) / 0.35),
      inset 0 -1px 0 0 hsl(var(--primary) / 0.18),
      0 16px 38px -18px hsl(var(--primary) / 0.45),
      0 6px 16px -6px hsl(var(--primary) / 0.30);
  }

  .btn-pricing-featured:hover {
    border-color: hsl(var(--primary) / 0.60);
    background:
      radial-gradient(800px 300px at 50% 0%, hsl(var(--primary) / 0.30), transparent 70%),
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.28) 0%,
        hsl(var(--primary) / 0.18) 52%,
        hsl(var(--primary) / 0.14) 100%
      ),
      linear-gradient(135deg, hsl(var(--primary) / 0.35), hsl(var(--accent) / 0.28));
    box-shadow:
      inset 0 1px 0 0 hsl(var(--primary) / 0.40),
      inset 0 -1px 0 0 hsl(var(--primary) / 0.22),
      0 20px 46px -18px hsl(var(--primary) / 0.55),
      0 8px 20px -6px hsl(var(--primary) / 0.35);
  }

  /* Current plan variant - disabled state */
  .btn-pricing-current {
    border-color: hsl(var(--muted) / 0.40);
    background:
      linear-gradient(
        180deg,
        hsl(var(--muted) / 0.15) 0%,
        hsl(var(--muted) / 0.10) 52%,
        hsl(var(--muted) / 0.06) 100%
      ),
      hsl(var(--card));
    color: hsl(var(--muted-foreground));
    box-shadow:
      inset 0 1px 0 0 hsl(var(--muted) / 0.20),
      inset 0 -1px 0 0 hsl(var(--muted) / 0.10),
      0 8px 20px -12px hsl(var(--muted) / 0.25);
    cursor: default;
  }

  .btn-pricing-current:hover {
    transform: none;
    filter: none;
    border-color: hsl(var(--muted) / 0.40);
    background:
      linear-gradient(
        180deg,
        hsl(var(--muted) / 0.15) 0%,
        hsl(var(--muted) / 0.10) 52%,
        hsl(var(--muted) / 0.06) 100%
      ),
      hsl(var(--card));
    box-shadow:
      inset 0 1px 0 0 hsl(var(--muted) / 0.20),
      inset 0 -1px 0 0 hsl(var(--muted) / 0.10),
      0 8px 20px -12px hsl(var(--muted) / 0.25);
  }

  /* Outline variant - for downgrade */
  .btn-pricing-outline {
    border-color: hsl(var(--border));
    background:
      linear-gradient(
        180deg,
        hsl(var(--accent) / 0.12) 0%,
        hsl(var(--accent) / 0.08) 52%,
        hsl(var(--accent) / 0.04) 100%
      ),
      hsl(var(--card));
    color: hsl(var(--foreground));
    box-shadow:
      inset 0 1px 0 0 hsl(var(--accent) / 0.15),
      inset 0 -1px 0 0 hsl(var(--accent) / 0.08),
      0 8px 20px -12px hsl(var(--accent) / 0.20);
  }

  .btn-pricing-outline:hover {
    border-color: hsl(var(--primary) / 0.30);
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.15) 0%,
        hsl(var(--primary) / 0.10) 52%,
        hsl(var(--primary) / 0.06) 100%
      ),
      hsl(var(--card));
    color: hsl(var(--primary));
    box-shadow:
      inset 0 1px 0 0 hsl(var(--primary) / 0.20),
      inset 0 -1px 0 0 hsl(var(--primary) / 0.12),
      0 12px 28px -14px hsl(var(--primary) / 0.25);
  }

  /* Dark mode adjustments */
  .dark .btn-pricing {
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.16) 0%,
        hsl(var(--primary) / 0.10) 52%,
        hsl(var(--primary) / 0.06) 100%
      ),
      hsl(var(--sidebar));
    border-color: hsl(var(--primary) / 0.30);
    box-shadow:
      0 16px 40px -22px hsl(var(--primary) / 0.32),
      inset 0 1px 0 0 hsl(var(--primary) / 0.18);
  }

  .dark .btn-pricing:hover {
    border-color: hsl(var(--primary) / 0.40);
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.20) 0%,
        hsl(var(--primary) / 0.14) 52%,
        hsl(var(--primary) / 0.10) 100%
      ),
      hsl(var(--sidebar));
  }

  .dark .btn-pricing-primary {
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.85) 0%,
        hsl(var(--primary) / 0.70) 52%,
        hsl(var(--primary) / 0.60) 100%
      );
  }

  .dark .btn-pricing-primary:hover {
    background:
      linear-gradient(
        180deg,
        hsl(var(--primary) / 0.90) 0%,
        hsl(var(--primary) / 0.80) 52%,
        hsl(var(--primary) / 0.70) 100%
      );
  }
}

/* Scrollbar hide utility */
.scrollbar-hide {
  /* Firefox */
  scrollbar-width: none;
  /* Safari and Chrome */
  &::-webkit-scrollbar {
    display: none;
  }
}